[pytest]
asyncio_mode = auto
pythonpath = src
testpaths = src/tests
addopts = -m "not e2e"
markers =
    e2e: browser E2E tests (requires pytest-playwright)
    panel_auth: tests that require panel authentication enabled
